// Isabel Ovalles
// p4.cpp

const string FILENAME = "/home/fac/sreeder/class/cs1420/lab7input.dat";
const int MAX_NUMS = 30;

bool readFile(int a[], int b[], int c[])
// reads in file of integers

int main()
{
  int evenNum[MAX_NUMS];
  int oddNum[MAX_NUMS];
  int negNum[MAX_NUMS];
  bool ok = false;
  ok = readfile(evenNum[], oddNum[], negNum[]);

  while(ok) {
    for
  }
}

bool readFile(int a[], int b[], int c[]) {
  int currentInt;
  ifStreaminFile;
  inFile.open(FILENAME);
  if(inFile.fail()) {
    return false;
  }



  return true;
}
